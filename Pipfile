[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]

[packages]
requests = "==2.31"
coverage = "~=7.4"

[requires]
python_version = "3.10"

[scripts]
cover = 'coverage run -m unittest discover -v'
report = 'coverage report --include=lib/\*'
test = 'python -m unittest discover -v'
